<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="handler"
            type="ezlife.movil.oneparkingapp.fragments.CarsFragment" />
    </data>

    <android.support.design.widget.CoordinatorLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxWidth="@dimen/dialog"
        android:minWidth="@dimen/dialog">

        <LinearLayout
            android:layout_width="@dimen/dialog"
            android:layout_height="@dimen/dialog"
            android:orientation="vertical">

            <LinearLayout
                android:id="@+id/header"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/colorPrimary"
                android:orientation="vertical"
                android:padding="@dimen/medium">

                <TextView
                    style="@style/TextAppearance.AppCompat.Headline"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/dialog_car_title"
                    android:textColor="@color/textPrimaryInvert" />

                <TextView
                    style="@style/Base.TextAppearance.AppCompat.Body1"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/dialog_car_subtitle"
                    android:textColor="@color/textPrimaryLessInvert" />


            </LinearLayout>


            <android.support.v7.widget.RecyclerView
                android:id="@+id/list"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />


        </LinearLayout>

        <android.support.design.widget.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/small"
            android:onClick="@{()->handler.goToMyCars()}"
            app:fabSize="normal"
            app:layout_anchor="@id/header"
            app:layout_anchorGravity="bottom|right"
            app:srcCompat="@drawable/ic_setup" />

    </android.support.design.widget.CoordinatorLayout>
</layout>

