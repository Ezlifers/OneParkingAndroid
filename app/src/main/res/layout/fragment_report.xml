<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="loader"
            type="ezlife.movil.oneparkingapp.util.Loader" />
    </data>

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="@dimen/dialog"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <FrameLayout
                android:id="@+id/btnImg"
                android:layout_width="match_parent"
                android:layout_height="@dimen/report_img">

                <ImageView
                    android:id="@+id/img"
                    android:scaleType="centerCrop"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="@color/imgEmpty" />

                <ImageView
                    android:layout_width="@dimen/report_img_icon"
                    android:layout_height="@dimen/report_img_icon"
                    android:layout_gravity="center"
                    app:srcCompat="@drawable/ic_add_photo" />

                <ProgressBar
                    android:id="@+id/progressBar2"
                    style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Small"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="right|bottom"
                    android:padding="@dimen/small"
                    android:visibility="@{loader.loading ? View.VISIBLE: View.GONE}" />

            </FrameLayout>

            <EditText
                android:id="@+id/description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/msmall"
                android:hint="@string/report_description" />

            <TextView
                android:id="@+id/btnReport"
                style="@style/ButtonReportNormal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:enabled="@{!loader.loading}"
                android:text="@{loader.loading? @string/sending :@string/report}" />

        </LinearLayout>


    </FrameLayout>
</layout>

